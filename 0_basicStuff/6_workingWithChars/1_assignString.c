#include <stdio.h>
#include <string.h>

int main(){
    char assignExample[20];

    // ? how to assign strings!
    strcpy(assignExample, "example");

    printf("%s \n", assignExample);

    return 0;
}
